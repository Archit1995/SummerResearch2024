
early_termination:=proc(function)
    local i,j:
    if function = mqrfr then 
        # print(function):
    end if:
end proc:  

# handler:=proc(function)
#      if function = F then 
#          print("function is F");
#      elif function = G then 
#          print("G");
#      end if;
#  end proc: